version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: breathe_esg
      POSTGRES_USER: breathe_user
      POSTGRES_PASSWORD: breathe_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
